function Login()
{
	var user = document.getElementById('login_username').value;
	var pass = document.getElementById('login_password').value;
	
	// This alert should be replaced with a proper login check
	alert('You logged in with username "' + user + '" and password "' + pass + '".');
}